# pcmanfm-qt draws the LXQt desktop. Installed to /etc/xdg, not seeded into
# $HOME, so it survives a mounted home directory.
# WallpaperMode=none draws no image, just BgColor.

[Desktop]
WallpaperMode=none
BgColor=#000000
FgColor=#ffffff
ShadowColor=#000000
ShowHidden=false
